// Shared constants between ShopPlanner.tsx, its drag hook, and the presentational
// floor-plan layers — kept together so a single source of truth avoids a circular
// import between ShopPlanner.tsx and its subcomponents.
export const SCALE = .094
export const DRAW_ZONE_LABEL = 'Out-of-bounds zone'
